<div class="mb-6 flex items-center gap-3">
  <h1 class="text-2xl font-bold tracking-tight">Incoming orders</h1>
  <span class="pill flex items-center gap-1.5">
    <span class="h-1.5 w-1.5 animate-pulse rounded-full bg-emerald-600"></span> Live
  </span>
</div>

<%= turbo_stream_from "incoming_orders" %>
<div id="incoming_orders" class="grid gap-4 sm:grid-cols-2 lg:grid-cols-3">
  <%= render @orders %>
</div>
